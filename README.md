# Project Name

This is the replicate of the SLIT model with modern pytorch version. More updates are coming
Note: This Repo does not contain any dataset due to privacy issue.
## Environment Setup

This project uses **Conda**.

```bash
conda env create -f environment.yml
conda activate SLIT
